language: cpp

compiler:
  - gcc
  - clang

os: osx

osx_image: xcode7.3

before_install:
  - brew update
  - brew install cmake
  - brew install cpprestsdk
  - brew link --force openssl
  - if [ "$CXX" = "g++" ]; then brew install gcc; CXX=g++-5; fi

script:
  - cmake . -DCMAKE_BUILD_TYPE=Release && make
